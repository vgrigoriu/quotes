import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Daily Wisdom")
                .font(.largeTitle)
                .padding()

            Text("Hello, World!")
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
